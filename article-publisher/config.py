# Configuration file for the article publisher

# API details for the chosen publishing platform
API_KEY = "your_api_key"
API_SECRET = "your_api_secret"

# Details of the publishing platform
PUBLISHING_PLATFORM_URL = "https://publishing-platform-url.com"
PUBLISHING_PLATFORM_USERNAME = "your_username"
PUBLISHING_PLATFORM_PASSWORD = "your_password"

# Path to the directory where the articles to be published are stored
ARTICLES_DIRECTORY = "../article-generator/articles"

# Other configuration settings can be added as needed
# For example, you might want to specify the format of the articles (e.g., .txt, .docx, etc.)
# Or you might want to specify a schedule for when articles should be published